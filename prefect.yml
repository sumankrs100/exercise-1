http:
  routers:
    prefect:
      entryPoints:
        - web
      rule: "Host(`your-traefik-domain.com`)"
      service: prefect

  services:
    prefect:
      loadBalancer:
        servers:
          - url: "http://prefect-vm-ip:4200"
